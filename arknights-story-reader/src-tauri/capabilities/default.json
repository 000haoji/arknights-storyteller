{
  "$schema": "../gen/schemas/capabilities.json",
  "identifier": "default",
  "description": "Default capabilities for the application",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ],
  "allow": [
    { "cmd": "plugin:apk-updater|download_and_install" },
    { "cmd": "plugin:apk-updater|open_install_permission_settings" }
  ]
}
